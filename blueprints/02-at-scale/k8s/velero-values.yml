# Copyright (c) CloudBees, Inc.

# https://artifacthub.io/packages/helm/vmware-tanzu/velero
# https://github.com/vmware-tanzu/helm-charts/blob/main/charts/velero/values.yaml

nodeSelector:
  kubernetes.io/os: linux

kubectl:
  image:
    # Issue #60's original pin (1.26.14-debian-11-r6) no longer exists --
    # Bitnami's 2025 registry policy change removed nearly all historical
    # version tags from the free tier, leaving only "latest" pullable.
    tag: latest
